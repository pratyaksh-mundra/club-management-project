create table employee
(
    eid          INT     not null
        primary key,

    desiganation VARCHAR not null,
    salary       INT     not null,
    mid          INT     not null
        references manager
);